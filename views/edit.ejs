<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= book.title %> - Edit Entry</title>
    <!-- Bootstrap Link for Styling-->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <!--Navbar Header, that displays Title + Offers a Back To List Button-->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary p-3 shadow-sm">
      <div class="container-fluid">
        <a class="navbar-brand me-4 d-flex align-items-center" href="/">
          <span class="fs-4 me-2">✍️</span>
          Edit Manga: <%= book.title %>
        </a>

        <div class="ms-lg-auto">
          <a
            href="/"
            class="btn btn-light"
            style="text-decoration: none; color: black"
            >Back to List</a
          >
        </div>
      </div>
    </nav>

    <div class="container mt-5">
      <h2 class="mb-4">Update Details</h2>
      <!-- Form for Input Values - All of the Inputs have preset Values, based on the Values from the DB-->
      <form action="/edit/<%= book.id %>" method="POST" class="edit-form">
        <input type="hidden" name="id" value="<%= book.id %>" />

        <div class="mb-3">
          <label for="title" class="form-label">Title:</label>
          <input
            type="text"
            class="form-control"
            id="title"
            name="title"
            value="<%= book.title %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="author" class="form-label">Author:</label>
          <input
            type="text"
            class="form-control"
            id="author"
            name="author"
            value="<%= book.author %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="rating" class="form-label">Rating (1-10):</label>
          <input
            type="number"
            class="form-control"
            id="rating"
            name="rating"
            value="<%= book.rating %>"
            min="1"
            max="10"
            required
          />
        </div>

        <div class="mb-3">
          <label for="notes" class="form-label">Notes / Summary:</label>
          <textarea class="form-control" id="notes" name="notes" rows="5">
<%= book.notes %></textarea
          >
        </div>

        <div class="mb-4">
          <label for="isbn" class="form-label"
            >ISBN (13-digits, for Coverimage):</label
          >
          <input
            type="text"
            class="form-control"
            id="isbn"
            name="isbn"
            value="<%= book.isbn %>"
            maxlength="13"
          />
        </div>

        <button type="submit" class="btn btn-success btn-lg saveBtn">
          Save Changes
        </button>
      </form>

      <hr class="my-5" />

      <!-- Delete Button Area-->
      <div class="alert alert-danger shadow-sm p-4" role="alert">
        <h3 class="delete-warning">Remove Entry</h3>
        <p>Caution: This Process can not be reversed.</p>
        <form
          action="/delete/<%= book.id %>"
          method="POST"
          class="delete-form d-inline"
        >
          <button type="submit" class="btn btn-danger large-delete-btn">
            Remove Entry completely
          </button>
        </form>
      </div>
    </div>
    <!-- Bootstrap JS Script Connection-->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
