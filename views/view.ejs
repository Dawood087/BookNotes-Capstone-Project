<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= book.title %> - Details</title>
    <!-- Bootstrap Link for Styling-->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body class="bg-light">
    <!--Navbar Header, that displays Title + Offers a Back To List Button-->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary p-3 shadow-sm">
      <div class="container-fluid">
        <a class="navbar-brand me-4 d-flex align-items-center" href="/">
          <span class="fs-4 me-2">📚</span>
          View Details: <%= book.title %>
        </a>
        <div class="ms-lg-auto">
          <a
            href="/"
            class="btn btn-light"
            style="text-decoration: none; color: black"
            >Back to List</a
          >
        </div>
      </div>
    </nav>

    <!-- Card that holds all the Info - Displays the Info based on DB Values-->
    <div class="container mt-5">
      <div class="card shadow-lg">
        <div class="row g-0">
          <div class="col-md-4">
            <% if (book.isbn) { %>
            <img
              class="img-fluid rounded-start coverImage"
              src="https://covers.openlibrary.org/b/isbn/<%= book.isbn %>-L.jpg"
              alt="Cover for <%= book.title %>"
              style="height: 100%; width: 100%; object-fit: cover"
            />
            <% } else { %>

            <!-- Display Placeholder Cover, if isbn has no value-->
            <img
              class="img-fluid rounded-start coverImage"
              src="https://static.wixstatic.com/media/a7a45a_56f23830b5ef4a80a0d6387712433ca5~mv2.jpg"
              alt="Placeholder Cover"
              style="height: 100%; width: 100%; object-fit: cover"
            />
            <% } %>
          </div>

          <div class="col-md-8">
            <div class="card-body">
              <h1 class="card-title text-primary" id="title">
                <%= book.title %>
              </h1>
              <p class="card-text text-muted lead mb-4" id="author">
                by <%= book.author %>
              </p>

              <hr />

              <h4 class="mb-3">
                <span class="badge bg-light text-dark border p-2">
                  ⭐ Rating: <%= book.rating %> / 10
                </span>
              </h4>

              <h4 class="mt-4 text-secondary" id="notes-caption">
                Notes / Summary:
              </h4>
              <p class="card-text fs-5 p-3 rounded border" id="notes">
                <%= book.notes %>
              </p>

              <p class="card-text mt-4 fs-6 text-muted">
                ISBN: <span id="isbn"><%= book.isbn %></span>
              </p>

              <hr class="mt-4 mb-4" />

              <div class="edit-area">
                <h2 class="h5 mb-3">If you want to make any changes:</h2>

                <!-- Edit Redirect Buttton-->
                <a href="/edit/<%= book.id %>" class="btn btn-primary btn-lg">
                  Edit Entry
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <!-- Bootstrap JS Script Connection-->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
